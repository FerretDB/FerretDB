net:
  port: 47017 # matches host's port for replica set to work
  bindIpAll: true
  tls:
    mode: disabled

security:
  authorization: disabled

replication:
  oplogSizeMB: 512
  replSetName: mongodb-rs
